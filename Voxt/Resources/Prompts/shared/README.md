# Shared Prompts

Model-specific prompt templates that are intentionally independent of the app interface language.

These files contain protocol-level wording for local ASR models such as MOSS. Keep user-facing localized presets in the language directories instead.
